{
  "name": "github-actions-aws-lambda-updater",
  "version": "1.0.0",
  "description": "AWS lambda updater",
  "main": "index.js",
  "scripts": {
    "build": "ncc -m -s build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ConvergeStack",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@aws-sdk/client-lambda": "^3.309.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "standard": "^17.0.0"
  }
}
